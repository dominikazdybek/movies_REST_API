# movies_REST_API

The aim of the workshop was to write a backend function for the REST application. The application supports the list of movies. It is written using the Django frameworks and Django REST Framework, implementing the REST functionality.
